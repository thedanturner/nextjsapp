import { Metadata } from "next";

export const metadata:Metadata = {
  title: 'WeatherApp',
};

const weatherapp = () => {
  return <div>Weather App Holding Page</div>;
};


export default weatherapp;